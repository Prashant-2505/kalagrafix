import React from 'react'
import { Login } from './Login'
export const metadata = {
    title: "KalaGrafix | Login",
};
const page = () => {
    return (
        <Login />
    )
}

export default page
